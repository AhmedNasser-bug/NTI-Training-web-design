
/**
 * Checks for iterability
 * @param {any} variable the checked variable obv
 * @returns {boolean} wether the given variable is an iterable
 */
function isIterable(variable){
    return typeof(word) == typeof("str") || typeof(word) == typeof([])
}